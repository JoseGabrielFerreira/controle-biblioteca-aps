<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControleBibliotecaAps">

        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activity.MenuActivity"
            android:exported="false" />

        <activity
            android:name=".activity.MenuUsuarioActivity"
            android:exported="false" />

        <activity
            android:name=".activity.LivrosActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ListaLivrosActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ClientesActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ListaClientesActivity"
            android:exported="false" />

        <activity
            android:name=".activity.EmprestimoActivity"
            android:exported="false" />

        <activity
            android:name=".activity.ListaEmprestimosActivity"
            android:exported="false" />

        <activity android:name=".activity.DevolverLivroActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>